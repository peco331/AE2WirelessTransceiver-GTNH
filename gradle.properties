# ExampleMod tag to use as Blowdryer settings version. Kept empty so ordinary builds do not depend on
# raw.githubusercontent.com; the convention plugin's local Checkstyle rule remains enabled below.
# LOCAL to test local config updates.
gtnh.settings.blowdryerTag =

# Human-readable mod name, available for mcmod.info population.
modName = AE2 Wireless Transceiver (GTNH)

# Case-sensitive identifier string, available for mcmod.info population.
# Conventionally lowercase.
modId = ae2wtx

# Root package of the mod.
modGroup = cn.gtnh.ae2wtx

# Whether to use modGroup as the maven publishing group.
useModGroupForPublishing = true

# Updates your build.gradle and settings.gradle automatically whenever an update is available.
autoUpdateBuildScript = false

# Version of Minecraft to target
minecraftVersion = 1.7.10

# Version of Minecraft Forge to target
forgeVersion = 10.13.4.1614

# Specify an MCP channel for dependency deobfuscation and the deobfParams task.
channel = stable

# Specify an MCP mappings version for dependency deobfuscation.
mappingsVersion = 12

# Defines other MCP mappings for dependency deobfuscation.
remoteMappings = https\://raw.githubusercontent.com/MinecraftForge/FML/1.7.10/conf/

# Select a default username for testing your mod.
developmentEnvironmentUserName = Developer

# Enables modern Java syntax support.
enableModernJavaSyntax = jabel

# Generate a class with a String field for the mod version named as defined below.
generateGradleTokenClass = cn.gtnh.ae2wtx.Tags

# Name of the token containing the project's current version to generate/replace.
gradleTokenVersion = VERSION

# Specifies the configuration file for Forge's access transformers here.
accessTransformersFile =

# Provides setup for Mixins if enabled. If you don't know what mixins are: Keep it disabled!
usesMixins = true

# Package that contains the mixin classes, RELATIVE to modGroup
# (full package cn.gtnh.ae2wtx.mixin; must match "package" in mixins.ae2wtx.json).
mixinsPackage = mixin

# Add CurseMaven, Modrinth, and some more well-known 1.7.10 repositories.
includeWellKnownRepositories = true

# A list of repositories to exclude from includeWellKnownRepositories.
excludeWellKnownRepositories =

# Maven repository to publish the mod to (not used for local builds).
usesMavenPublishing = false

# Spotless obtains its rules through Blowdryer, so keep it off for an offline-capable build.
disableSpotless = true

# Keep Checkstyle enabled; GTNHConvention supplies its local wildcard-import rule without Blowdryer.
# disableCheckstyle = true

org.gradle.configuration-cache = true
org.gradle.parallel = true
