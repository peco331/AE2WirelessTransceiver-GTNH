# 详细更新日志

这里记录每个版本的完整改动。GitHub Release 页面只保留玩家最关心的更新摘要；实现原理与当前架构另见 [技术文档](docs/TECHNICAL.md)。

## v1.0.6

发布日期：2026-08-23

### 玩家功能

- 频道占用改为沿收发器所在分支的真实 AE2 导线连接统计。所有带 `REQUIRE_CHANNEL` 的设备都会计入，即使设备因超载而处于“缺少频道”状态，因此 33 台设备会准确显示红色 `33/32`。
- AE2 多方块设备的节点集合会折叠为一次频道需求；其他无线收发器、频段虚拟节点和 ME Controller 作为统计边界，不会把别的导线分支误算进来。
- 在频段列表中点选频段即可查看其在线数、收发器数和频道状态，无需先绑定；点击列表空白、搜索框或右侧区域可以取消选择。
- 频段搜索与列表改为服务端分页，频段数量较多时不再一次传输完整列表。
- 新增每位所有者和整个世界的频段创建上限，默认分别为 `128` 与 `4096`。上限只限制新建频段，降低配置不会删除已有频段。
- 专用服务器修改功耗或频段上限后，可由管理员执行 `/ae2wtx reload` 立即加载；跨维度开关仍需完整重启。
- 锁定的收发器现在也会阻止无权限玩家直接挖掘；删除整个频段仅允许收发器所有者或管理员。

### 修复与存档保护

- 修复 GUI 首次打开时列表请求使用错误窗口编号、导致列表暂时为空的问题。
- “新建”和“设定”遇到空白或无效名称时不再被误当成断开操作；创建失败时保留原有频段连接。
- 频段注册表加入数据版本与作用域标记，可迁移旧的全局或分维数据；切换跨维作用域后，旧频段会按端点维度确定性合并或拆分。
- 加强方块本地标签与世界频段注册表之间的恢复和自修复：可修复重复或非法频道号、重复端点和部分损坏记录，单个坏端点不会拖垮整个频段。
- 方块标签缺失但注册表仍保留端点时，可从持久注册表恢复频段，避免区块加载或重启后出现显示标签丢失或连接状态不一致。
- 删除频段、锁定方块和扳手拆卸继续在服务端执行所有者与管理员权限检查。

### 兼容性与服务端安全

- GTNHLib 改为可选的增强模型集成，不再声明硬性最低版本。未安装或模型 API 不兼容时自动使用内置原版立方体外观，核心功能不受影响。
- Waila 改为可选兼容；缺失时只是不显示 Waila 信息。
- 所有世界与注册表操作改到服务端 tick 主线程执行，并加入距离、窗口、维度、请求编号、包大小、队列容量和每玩家速率限制。
- 删除操作使用有上限的批量数据包；畸形或过期响应会被丢弃，不会误清频段或覆盖当前 GUI。
- 不再读取或复制 AE2 私有安全键，无线连接统一通过 AE2 自身的安全检查。
- 世界或区块卸载时释放运行时虚拟节点并保留持久频段元数据，减少世界对象被长期引用的风险。

### 构建与维护

- 使用 JDK 25 与 Jabel 构建，发布 class 文件保持 Java 8 兼容级别 `52`。
- 新增自动构建、测试、代码检查和发布产物字节码级别验证。
- 新增 16 项回归测试，覆盖超载 `33/32`、循环与无线边界、多方块折叠、分页协议、损坏数据、作用域迁移和端点所有权等场景。
- 发布 JAR 内附项目许可证和第三方资源归属说明。

[查看 v1.0.5...v1.0.6 的代码差异](https://github.com/peco331/AE2WirelessTransceiver-GTNH/compare/v1.0.5...v1.0.6)

## v1.0.5

发布日期：2026-08-23

### 修复与存档保护

- 区分区块临时卸载与方块永久拆除：区块卸载时保留频段端点记录。
- 删除频段时，已加载的收发器立即断开；未加载区块中的收发器记录持久化待清除标记，未来加载后自动清除旧绑定，防止已删除频段复活。
- 待清除记录同时校验所有者、维度、坐标和频段名，避免坐标复用或不同玩家的同名频段被误清。
- 频段统计跳过未加载区块，不会为了统计而主动加载区块。
- 跨维度开关改为重启后生效，避免运行中改变注册表作用域造成混合状态；空闲功耗仍可即时修改。
- 数据包解码严格区分玩家主动断开所需的空字符串与畸形数据，异常数据包会被直接丢弃。

[查看 v1.0.4...v1.0.5 的代码差异](https://github.com/peco331/AE2WirelessTransceiver-GTNH/compare/v1.0.4...v1.0.5)

## v1.0.4

发布日期：2026-08-23

### 稳定性与权限

- 修复关闭跨维无线时，把维度 ID 当成服务器世界数组下标所导致的下界、末地及 Mod 维度越界异常。
- AE2 网络可视化工具只在当前视角维度合成无线连线，避免其他维度的同坐标收发器被误识别。
- 同频段收发器会在网络可视化工具中显示确定的致密无线拓扑连线与频道负载；这只影响展示，不改变底层 AE2 网格。
- 频道统计由方块坐标近似扫描改为遍历真实 `IGridConnection` 图，提高本机导线分支统计准确性。
- 锁定收发器的拆卸、解锁和频段管理在服务端强制校验，仅拥有者与管理员可操作；未锁定收发器保持开放。
- GUI 容器与操作数据包加入 8 格距离检查和字符串边界校验。
- 全频段频道总数与在线收发器数使用共享缓存，避免同一频段的每台设备重复扫描全部端点。
- 编译依赖对齐 GTNH 2.9.0-beta2 使用的 AE2 `rv3-beta-1034-GTNH`。

[查看 v1.0.3...v1.0.4 的代码差异](https://github.com/peco331/AE2WirelessTransceiver-GTNH/compare/v1.0.3...v1.0.4)

## v1.0.3

发布日期：2026-08-20

### 显示修复

- 修复挖掘或破坏无线收发器时，粒子错误显示为缩小的 UV 展开图。
- 为在线和离线状态分别使用完整的 128×128 粒子贴图并修正模型粒子纹理映射。

[查看 v1.0.2...v1.0.3 的代码差异](https://github.com/peco331/AE2WirelessTransceiver-GTNH/compare/v1.0.2...v1.0.3)

## v1.0.2

发布日期：2026-08-17

### 模型、交互与连接修复

- 加入基于 GTNHLib JSON 模型系统的 Light Mode 3D 外观、在线呼吸发光动画和方块发光状态。
- 修复物品栏、掉落物和方块模型的透明、缺面、黑紫缺失模型及纹理路径问题，并加入 `pack.mcmeta`。
- 修复收发器贴近 ME 线缆时的连接建立和线缆连接外观刷新，避免重复刷新导致线缆掉落。
- 禁止在客户端创建 AE2 网格节点，修复相关客户端崩溃。
- 相邻无线收发器不再直接互连，避免频道串线；同时让 AE2 网络可视化工具能够识别收发器节点。
- 频段 GUI 支持普通点击单选、`Ctrl` 点击多选并批量删除频段。
- 更新模型、材质、原项目和贡献者的文档与版权归属。

[查看 v1.0.1...v1.0.2 的代码差异](https://github.com/peco331/AE2WirelessTransceiver-GTNH/compare/v1.0.1...v1.0.2)

## v1.0.1

发布日期：2026-08-15

### 首次发布

- 将 ExtendedAE_Plus 的无线收发器移植为 GTNH 1.7.10 独立 MOD。
- 支持通过命名频段无线桥接 ME 频道，并提供可配置的跨维度连接。
- 提供频段新建、删除、设定、断开、搜索和状态信息 GUI。
- 提供本机及全频段频道占用、Waila 状态、在线收发器数量和满载/超载颜色提示。
- 支持扳手锁定、权限保护、潜行拆卸和相邻收发器防串线。
- 提供量子环与量子链接相关的合成配方。
- 初始目标环境为 GTNH 2.9.0-beta1；客户端与服务端需要安装相同版本。
